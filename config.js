/* ============================================================
   config.js — 사이트 설정 (이름·연락처·시리즈 색상)
   ------------------------------------------------------------
   이 파일은 데이터(data.js)와 분리돼 있어, 시트를 다시 변환해도
   덮어쓰이지 않습니다. 여기 값만 고치면 사이트에 바로 반영됩니다.
   ============================================================ */

window.SITE = {
  defaultLang: 'kr',                       // 처음 열렸을 때 언어 'kr' 또는 'en'
  title: { kr: '최가영', en: 'Kayoung Choi' },

  contact: {
    email: 'your@email.com',               // ← 실제 이메일로 변경
    instagram: 'your_instagram'            // ← @ 없이 아이디만. 안 쓰면 '' 로
  }
};

/* 시리즈별 아이덴티티 색상.
   여기서만 고치면 지도·이미지·텍스트 화면에 모두 반영됩니다.
   키(왼쪽)는 시리즈 이름(한국어)과 정확히 일치해야 합니다.
   어떤 시리즈를 비워두거나 빼면, 그 시리즈 작품이 속한
   리서치 색을 자동으로 사용합니다. */
window.SERIES_COLORS = {
  '캔드 랜드스케이프': '#1589ff',
  '세르비아의 산':     '#967bb6',
  '판타지 속 생존':    '#69beff',
  '후루츠':           '#ff6ba1',
  '오프너':           '#ff7900'
};
